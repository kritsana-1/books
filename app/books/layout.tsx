export const metadata = {
  title: 'Books | BookHub Community',
  description: 'Discover, share, and discuss books with our community.',
};

export default function BooksLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
