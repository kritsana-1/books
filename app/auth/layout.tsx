export const metadata = {
  title: 'Sign In | BookHub Community',
  description: 'Sign in to your BookHub Community account',
};

export default function AuthLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
