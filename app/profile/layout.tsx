export const metadata = {
  title: 'Profile | BookHub Community',
  description: 'Manage your BookHub Community profile',
};

export default function ProfileLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
